upstream uwsgi {
    server unix:/tmp/uwsgi/admin_app.sock;
}
server {
    listen      80;
    server_name 127.0.0.1;
    charset     utf-8;
    location /static {
        alias /var/www/html/static;
    }
    location / {
        uwsgi_pass  uwsgi;
        include     uwsgi_params;
    }
}